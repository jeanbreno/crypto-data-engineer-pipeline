name: 'crypto_pipeline'
version: '1.0'
config-version: 2

profile: 'crypto_pipeline'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  crypto_pipeline:

    staging:
      materialized: view

    marts:
      materialized: table