crypto_pipeline:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: airflow
      password: airflow
      port: 5432
      dbname: warehouse
      schema: analytics
      threads: 1